when:
  - event: [push, manual]
    branch: develop

steps:
  - name: build
    image: ubuntu:22.04
    commands:
      - apt update && apt install -y build-essential libmpfr-dev
      - mkdir build && cd build
      - cmake -DCMAKE_BUILD_TYPE=Debug ..
      - make -sj4